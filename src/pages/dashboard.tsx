import { Button } from "@/components/ui/button";

const Dashbaord = () => {
  return (
    <div>
      <div>DASHBOARD</div>
      <Button>LOGOUT</Button>
    </div>
  );
};

export default Dashbaord;
